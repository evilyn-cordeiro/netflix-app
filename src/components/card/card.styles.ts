import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    padding: 25,
    alignItems: "center",
  },
  avatar: {
    width: 85,
    height: 85,
    marginBottom: 4,
  },
  name: {
    color: "#fff",
    fontSize: 14,
    fontWeight: "400",
  },
});
